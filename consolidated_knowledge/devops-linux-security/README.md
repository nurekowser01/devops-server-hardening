# Linux Security & Hardening (`devops-linux-security`)

## Overview
Hardening the Ubuntu OS layer. 
- **Unattended-Upgrades:** Automated security patches.
- **Docker Socket:** Restrict `/var/run/docker.sock` to the `docker` group only.
- **Sysctl:** IP spoofing protection, ignoring ICMP redirects.
